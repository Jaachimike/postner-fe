API_BASE_URL=http://localhost:8001

# Origin serving post media, mirroring the API's STORAGE_PUBLIC_BASE_URL.
# Named in the CSP so preview images load (see next.config.ts) — the local
# stack runs MinIO over plain http, which a blanket `https:` source blocks.
STORAGE_PUBLIC_BASE_URL=http://localhost:9000
